#include<bits/stdc++.h>

using ll = long long;
using namespace std;

int main() {
    int t; cin >> t;

    while(t--) {
        ll n, h, k, s; cin >> n >> h >> k >> s;

        for (int i = 0; i < n; i++) {
            ll l, r; cin >> l >> r;

        }
    }
}